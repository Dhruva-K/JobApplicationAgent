# Job Application Agent Configuration

# Neo4j Database Configuration
neo4j:
  uri: "neo4j://127.0.0.1:7687"
  user: "neo4j"
  password: "CSCE689dk"  # Change this in production
  database: "jobAgent"

# LLM Configuration
llm:
  provider: "ollama"  # Options: "ollama", "vllm"
  model_name: "llama3:8b"
  base_url: "http://localhost:11434"  # Ollama default
  temperature: 0.7
  max_tokens: 2048

# Embeddings Configuration
embeddings:
  model: "sentence-transformers/all-MiniLM-L6-v2"
  device: "cpu"  # Options: "cpu", "cuda"

# Job APIs Configuration
job_apis:
  jsearch:
    api_key: "ak_36ki7wht1f4ly3zi5z6lyqvfcfu0zbq26n56to3ajjwei56"  # Add your JSearch API key
    base_url: "https://api.openwebninja.com/jsearch/"
  remotive:
    api_key: ""  # Add your Remotive API key if needed
    base_url: "https://remotive.com/api"

# Vector Store Configuration (optional)
vector_store:
  provider: "chromadb"  # Options: "chromadb", "none"
  persist_directory: "./chroma_db"

# Application Settings
app:
  max_jobs_per_search: 10
  match_threshold: 0.6
  enable_caching: true
  cache_ttl_hours: 24

# Streamlit UI Configuration
ui:
  page_title: "Job Application Agent"
  page_icon: "💼"

